# Pi Zero 2W (512 MB RAM) — every entry justified, nothing heavier allowed.
# Install on Pi OS Lite 64-bit via piwheels (prebuilt ARM wheels).
numpy>=1.24            # all v1 inference maths (Mahalanobis is one matvec)
scipy>=1.10            # filters, welch, stft
scikit-learn>=1.3      # KMeans + LedoitWolf in baseline.py only (~30 MB)
pyyaml>=6.0
paho-mqtt>=1.6         # 1.x and 2.x both supported (compat shim in mqtt_client.py)
sounddevice>=0.4       # ALSA capture (apt install libportaudio2 first)
spidev>=3.6            # IIS3DWB (hardware only; harmless elsewhere)
# NO TensorFlow anywhere on the device for v1.
# v1.5 (optional cloud AE): pip install tflite-runtime   # inference only, ~5 MB
